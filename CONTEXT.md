# 术语表

## 面板小程序

一个由 COSMIC 面板按 XDG `.desktop` 条目启动的独立 Rust 可执行程序；它不是被面板动态加载的 DLL。

## 指标源

提供一个可展示数值及其状态的外部数据适配器。网络吞吐和米家功率都属于指标源。

## 网络采样

从 Linux `/proc/net/dev` 的累计接收/发送字节计数，在相邻采样之间计算每秒速率。

## 米家本地协议

设备在局域网 UDP 54321 端口提供的 miIO 请求协议；请求载荷使用设备令牌派生的 AES-128-CBC 密钥和 IV 加密。

## _Avoid_

- “插件 DLL”：COSMIC 面板小程序不使用 TrafficMonitor 的 Windows DLL 加载契约。
- “通用米家功率”：不同型号的 SIID/PIID 不同，功率属性必须由配置明确指定。

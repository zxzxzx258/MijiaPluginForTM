# Custom-draw independent device items

Each smart plug remains an independent TrafficMonitor display item, but the plugin custom-draws every item as one line. This preserves TrafficMonitor's native two-items-per-column flow while ensuring an unpaired final device stays on the upper row instead of being reformatted as a two-line label/value column.
